import { transitions } from 'react-alert'
import { positions } from 'react-alert'

export const MAX_FAVORITES = 5
export const OPTIONS_ALERT = {
  position: positions.BOTTOM_CENTER,
  timeout: 5000,
  transition: transitions.SCALE,
  offset: '30px',
}
